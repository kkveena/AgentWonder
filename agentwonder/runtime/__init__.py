"""Runtime layer for the AgentWonder platform.

Provides workflow execution, session and state management, approval
handling, and model routing. The executor consumes a RuntimePlan
(produced by the compiler) and drives it through to completion.
"""
